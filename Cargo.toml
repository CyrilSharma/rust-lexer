[package]
name = "rust-lexer"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "A compiler that generates a Lexer using DFAs (inspired by flex)"

[lib]
name = "rflex_lib"
path = "src/lib.rs"

[[bin]]
name = "rflex"
path = "src/main.rs"